services:
  web:
    ports:
      - "8000:8090"
    restart: unless-stopped

  worker:
    restart: unless-stopped

  beat:
    restart: unless-stopped

  redis:
    restart: unless-stopped

  db:
    ports:
      - "5433:5432"
    restart: unless-stopped

volumes:
  pgdata:
    name: aqua_prod_pgdata
  media:
    name: aqua_prod_media
